CREATE TABLE sensor_data (
  sensor_id text,
  time timeuuid,
  type text,
  reading float, 
  presence boolean,
  PRIMARY KEY(sensor_id, time)
);

INSERT INTO sensor_data (sensor_id, time, type, reading, presence) VALUES ('0001', now(), 'TEMP', 23.0, false);
 